{
  "name": "wasmbox-render-test",
  "version": "0.0.0",
  "private": true,
  "description": "Headless-browser smoke test: serve the built wasmbox compositor and assert it actually rendered.",
  "license": "BSD-3-Clause",
  "type": "module",
  "scripts": {
    "test": "node render.mjs"
  },
  "devDependencies": {
    "playwright": "^1.49.0"
  }
}
